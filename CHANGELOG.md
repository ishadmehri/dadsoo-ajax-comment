# Changelog

همهٔ تغییرات مهم این پروژه در این فایل ثبت می‌شوند.

## [4.0.1] - 2026-09-22

### Fixed

نتیجهٔ اولین دور بررسی رسمی مخزن وردپرس (ایمیل تیم Plugins):

- **`Plugin URI` و `Author URI` یکسان بودند.** مخزن وردپرس این را با خطای صریح رد می‌کند — این دو باید به دو صفحهٔ متفاوت اشاره کنند. `Plugin URI` حالا به مخزن گیت‌هاب افزونه اشاره می‌کند؛ `Author URI` همان `elinweb.ir` ماند.
- **فایل‌های ترجمهٔ بسته‌شده (`.po`/`.mo`) از افزونه حذف شد.** افزونه‌های میزبانی‌شده در مخزن وردپرس نباید ترجمه همراه خودشان بفرستند؛ ترجمه باید کاملاً از طریق [translate.wordpress.org](https://translate.wordpress.org/) مدیریت شود. فایل‌های ساخته‌شده (`fa_IR.po`/`.mo`) به پوشهٔ `translations/` در ریشهٔ مخزن منتقل شدند (بخشی از بستهٔ افزونه نیستند، فقط مرجع و منبع آیندهٔ ثبت در GlotPress). فایل `.pot` در `languages/` باقی ماند (فقط الگو، ترجمهٔ واقعی ندارد).
- **`load_plugin_textdomain()` حذف کامل شد**، نه فقط نگه‌داشتن مشروط. بررسی شد که مکانیزم JIT وردپرس (`_load_textdomain_just_in_time`) فقط پوشهٔ مرکزی `wp-content/languages/plugins/` را برای متای `.mo` می‌گردد، نه پوشهٔ `languages/` خودِ افزونه — یعنی برای اینکه سایتی مثل dadsoo.com همچنان فارسی ببیند، کافی است فایل `.mo` مستقیم در همان پوشهٔ مرکزی قرار بگیرد، بدون نیاز به هیچ فراخوانی از داخل افزونه. این مسیر مستقیماً روی سایت آزمایشی تأیید شد.

## [4.0.0] - 2026-09-21

### Fixed

- **رفع غلط تایپی «agax» به «ajax» در همهٔ شناسه‌های افزونه.** این غلط از همان اولین نسخهٔ افزونه (Elin Agax Comment) وجود داشت و در بازنویسی‌های بعدی هم دست‌نخورده ماند. اصلاح شد در: نام پوشه و فایل اصلی (`dadsoo-agax-comment` → `dadsoo-ajax-comment`)، نام کلاس (`Dadsoo_Agax_Comment` → `Dadsoo_Ajax_Comment`)، اکشن‌های AJAX (`dadsoo_agax_submit_comment` و بقیه)، nonce، کلیدهای متا (`dadsoo_agax_likes`/`dislikes`/`vote_*`)، کوکی‌ها، شورت‌کدها (`[dadsoo-agax-comments]`)، نام ویجت‌های Elementor، دامنهٔ ترجمه، شیء سراسری جاوااسکریپت (`dadsooAgaxComment`) و نام فایل‌های زبان.

### Changed

- شمارش لایک/دیسلایک و رأی هر بازدیدکننده از کلیدهای متای `dadsoo_agax_*` به `dadsoo_ajax_*` منتقل می‌شود؛ چون پیشوند رأی شامل شناسهٔ رأی‌دهنده در انتهای خودش است، این بار مهاجرت با یک `REPLACE()` روی پیشوند انجام می‌شود، نه تطبیق دقیق کلید مثل مهاجرت‌های قبلی.
- شورت‌کدهای `dadsoo-agax-*` و ویجت‌های Elementor با همان نام، همچون نسخه‌های قبلی، به‌صورت مخفی نگه داشته شده‌اند تا محتوای موجود از کار نیفتد. تابع `dadsoo_agax_comment()` هم به‌عنوان alias منسوخ باقی مانده.

### Breaking

- **نام پوشهٔ افزونه عوض شده است**، نه فقط فایل داخلی. برای به‌روزرسانی، افزونهٔ قدیمی باید غیرفعال و پوشهٔ جدید جایگزین شود (به بخش «رفع غلط تایپی» در `README.md` نگاه کنید). مهاجرت دادهٔ متا/کوکی، مثل نسخهٔ ۳٫۰، خودکار و در همان لحظهٔ فعال‌سازی اجرا می‌شود.

## [3.2.0] - 2026-09-20

### Changed

- **زبان مبدأ رشته‌های افزونه از فارسی به انگلیسی تغییر کرد.** همهٔ متن‌های داخل `__()`/`_e()`/`esc_html_e()` (پیام‌های AJAX، برچسب‌های فرم، کنترل‌های ویجت Elementor، صفحهٔ تنظیمات) اکنون انگلیسی‌اند تا افزونه با قواعد مخزن وردپرس (که رشتهٔ مبدأ باید انگلیسی باشد) هم‌خوان باشد.
- ترجمهٔ فارسی (`fa_IR`) کامل در `languages/dadsoo-agax-comment-fa_IR.po` و `.mo` اضافه شد؛ متن‌ها همان متن‌های فارسی نسخه‌های قبل هستند، پس روی سایت‌های فارسی‌زبان (از جمله dadsoo.com که locale آن fa_IR است) هیچ تغییری در ظاهر افزونه دیده نمی‌شود.
- فایل `dadsoo-agax-comment.pot` به‌عنوان الگوی ترجمهٔ سایر زبان‌ها اضافه شد.
- سرتیتر افزونه فیلدهای `License: GPL v2 or later` و `License URI` گرفت؛ `Description` و `Author` هم انگلیسی شدند. این‌ها برای پذیرفته‌شدن در مخزن وردپرس الزامی‌اند.
- فایل `readme.txt` به فرمت استاندارد مخزن وردپرس اضافه شد (پیش‌تر افزونه فقط `README.md` داشت که مخزن وردپرس آن را نمی‌خواند).

### Fixed

نتیجهٔ اجرای افزونهٔ رسمی [Plugin Check](https://wordpress.org/plugins/plugin-check/) وردپرس روی این افزونه:

- `wp_cache_flush_group()` و `wp_cache_supports()` از وردپرس ۶.۱ اضافه شده‌اند، اما «Requires at least» افزونه ۵.۸ است. حالا وجود خودِ `wp_cache_flush_group` هم مستقیم بررسی می‌شود تا روی وردپرس قدیمی‌تر خطا ندهد (پیش‌تر عملاً امن بود، چون `wp_cache_supports` هم از همان نسخه اضافه شده و false برمی‌گرداند، ولی ابزار بررسی ایستا این را تشخیص نمی‌داد).
- بررسی نانس در ثبت نظر (نه پاسخ) با تابع اختصاصی `verify_nonce()` انجام می‌شد؛ به `check_ajax_referer()` هستهٔ وردپرس تغییر کرد (با نام فیلد سفارشی به‌عنوان آرگومان دوم) تا هم کد ساده‌تر شود و هم ابزارهای بررسی امنیتی آن را تشخیص دهند.
- خروجی ویجت فهرست نظرات با یک `echo` چندخطی چاپ می‌شد که کامنت `phpcs:ignore` انتهای آن را پوشش نمی‌داد؛ به یک متغیر میانی و `echo` تک‌خطی تغییر کرد.
- ورودی فیلد تلهٔ ربات اکنون علاوه بر `wp_unslash()` از `sanitize_text_field()` هم عبور می‌کند.

## [3.1.0] - 2026-09-05

### Added

- نشانگر بارگذاری. یک چرخانهٔ CSS (بدون تصویر) که رنگش از رنگ متنِ همان دکمه می‌آید: هنگام بارگذاری اولیهٔ فهرست، روی دکمهٔ «بارگذاری نظرات بیشتر»، و روی دکمهٔ ارسال نظر و پاسخ.
- حالت «هنوز نظری ثبت نشده است». پیش‌تر اگر نوشته‌ای هیچ دیدگاهی نداشت، هیچ‌چیز نمایش داده نمی‌شد.
- پس از «بارگذاری بیشتر»، صفحه روی اولین نظرِ دستهٔ تازه اسکرول می‌شود و روی آن فلَش می‌زند، تا کاربر بداند ادامه از کجاست. بارگذاری اول اسکرول نمی‌کند.
- کنترل «فاصله از بالا هنگام اسکرول» برای جبران هدر چسبانِ قالب (پیش‌فرض ۹۰ پیکسل، از طریق `--dadsoo-scroll-offset`).
- سه متن قابل تنظیم در ویجت Elementor و شورت‌کد: `load_more_text`، `loading_text` و `empty_text`. کنترلِ خالی به مقدار پیش‌فرض برمی‌گردد.
- بخش استایل «بارگذاری بیشتر» در Elementor: تایپوگرافی، رنگ متن و پس‌زمینه در حالت عادی و هاور، کادر، فاصلهٔ داخلی و گردی گوشه‌ها؛ به‌علاوهٔ رنگ و تایپوگرافی پیام‌های بارگذاری/خالی و رنگ فلَش.
- احترام به `prefers-reduced-motion`: چرخانه کندتر می‌چرخد، فلَش به یک کادر ثابت تبدیل می‌شود و اسکرول بدون انیمیشن انجام می‌گیرد.

### Fixed

- اسکرول نرم در بعضی مرورگرها و وب‌ویوها بی‌صدا نادیده گرفته می‌شود. حالا اگر بعد از یک لحظه هیچ حرکتی رخ نداده باشد، اسکرول بدون انیمیشن انجام می‌شود تا این قابلیت هیچ‌جا بی‌اثر نماند.

## [3.0.0] - 2026-09-05

### Changed

- **تغییر پیشوند از `elinweb`/`elin` به `dadsoo` در همهٔ شناسه‌ها.** نام فایل اصلی، کلاس‌ها، ثابت‌ها، اکشن‌های AJAX، nonce، کلیدهای متا، کوکی‌ها، شورت‌کدها، نام ویجت‌های Elementor، دامنهٔ ترجمه، شیء سراسری جاوااسکریپت و کلاس‌های CSS آیکون. مسیرهای سازگاری برای نسخه‌های قبل در بخش «ارتقا» از `README.md` فهرست شده‌اند.
- ثبت دیدگاه از `wp_insert_comment()` به `wp_new_comment()` منتقل شد، پس آکیسمت، کنترل سیل درخواست، فهرست کلمات ممنوع، قوانین بازبینی و ایمیل‌های اطلاع‌رسانی هستهٔ وردپرس هم اجرا می‌شوند. «تأییدشده» بودن دیگر از روی دسترسی کاربر حدس زده نمی‌شود، بلکه از وضعیت واقعی دیدگاه ثبت‌شده خوانده می‌شود.
- رنگ‌های پایه از `wp_add_inline_style` به `assets/css/style.css` منتقل شد تا قابل بازنویسی باشند.
- پاسخ `load_comments` کلید `has_more` هم برمی‌گرداند. اگر جاوااسکریپت افزونه را سفارشی کرده‌اید، این تغییر ناسازگار است.

### Security

- **رأی‌ها فقط با کوکی کنترل می‌شدند.** پاک‌کردن کوکی یا حالت ناشناس، رأی نامحدود می‌داد. حالا هر رأی با شناسهٔ پایدار رأی‌دهنده (شناسهٔ کاربر برای کاربر واردشده، و هش HMAC از IP و مرورگر با نمک سایت برای مهمان) در متای دیدگاه ثبت می‌شود؛ کوکی فقط نمای مرورگر را همگام نگه می‌دارد.
- تلهٔ ربات (honeypot) به فرم دیدگاه و فرم پاسخ اضافه شد.
- `wp_verify_nonce()` روی ورودی بدون `wp_unslash` اجرا می‌شد.
- متن دیدگاه اصلی با `wp_kses_post` (باز) و پاسخ با فهرست محدود فیلتر می‌شد؛ حالا هر دو از یک فهرست تگ مشترک استفاده می‌کنند.
- آدرس آیکون SVG که از سمت مرورگر برمی‌گردد فقط در صورتی پذیرفته می‌شود که روی همین دامنه باشد.
- شناسهٔ دیدگاه و عمق در خروجی HTML بدون `esc_attr()` چاپ می‌شدند.

### Performance

- **حذف N+1 کوئری.** رندر هر دیدگاه یک `get_comments()` جدا برای پاسخ‌هایش می‌زد. حالا کل درخت با یک کوئری به‌ازای هر سطح عمق (حداکثر ۴) گرفته می‌شود و متای همهٔ دیدگاه‌ها با یک `update_meta_cache()` پر می‌شود. تست یکپارچگی تعداد کوئری‌ها را می‌شمارد.
- CSS و JS روی همهٔ صفحات سایت بارگذاری می‌شدند. حالا فقط ثبت می‌شوند و صف‌شدنشان به رندر واقعی شورت‌کد یا ویجت موکول است.

### Fixed

- دکمهٔ «بارگذاری نظرات بیشتر» وقتی تعداد کل دقیقاً مضربی از اندازهٔ صفحه بود هرگز پنهان نمی‌شد. حالا سرور با `has_more` می‌گوید صفحهٔ بعدی وجود دارد یا نه.
- مبدأ صفحهٔ بعد با اندازهٔ صفحه جلو می‌رفت نه با تعداد واقعیِ برگشتی؛ و پس از درج نظر تازه در بالای فهرست تنظیم نمی‌شد، پس «بارگذاری بیشتر» آخرین نظر قبلی را تکراری می‌آورد.
- دو ویجت فرم روی یک صفحه، `id="dadsoo-comment-form"` تکراری و برچسب‌های `for` تکراری می‌ساختند. هر نمونه حالا شناسهٔ یکتای خودش را دارد و جاوااسکریپت به‌جای id روی کلاس متصل می‌شود.
- فرم پاسخ با id سراسری پیدا می‌شد؛ حالا از روی ساختار DOM پیدا می‌شود تا اگر یک نظر در دو ویجت روی یک صفحه بیاید، دکمه فرم درست را باز کند.
- آواتار با ایمیل دیدگاه درخواست می‌شد نه با خود آبجکت دیدگاه، پس آواتار سفارشی کاربران واردشده نادیده گرفته می‌شد.
- حلقهٔ «حدس‌زدن فیلد نظر» می‌توانست یک فیلد ناخواستهٔ فرم را به‌عنوان متن دیدگاه بردارد. حذف شد.
- پینگ‌بک و ترک‌بک به‌عنوان دیدگاه عادی در فهرست رندر می‌شدند.
- پیام‌های سرور با `html()` درج می‌شدند؛ حالا با `text()`.
- ارسال همزمان چند درخواست «بارگذاری بیشتر» با کلیک سریع، نظرات تکراری اضافه می‌کرد.
- متن‌های افزونه هاردکد بودند با اینکه `Text Domain` تعریف شده بود. همه از `__()` عبور می‌کنند و `load_plugin_textdomain()` صدا زده می‌شود.
- دکمه‌های رأی حالا `aria-pressed` دارند و دکمهٔ «بارگذاری بیشتر» `type="button"` گرفت.

## [2.3.1] - 2026-09-01

### Fixed

- آیکون SVG آپلودی (نه آیکون‌های کتابخانهٔ Elementor یا Font Awesome) رنگ کنترل استایل را نادیده می‌گرفت. چنین SVGی معمولاً روی هر مسیر یک `fill` صریح دارد؛ چون این مقدار روی خودِ عنصر است، جلوتر از وراثت `fill: currentColor` از `<svg>` والد قرار می‌گیرد. حالا فقط عناصر با `fill` صریح (به‌جز `fill="none"`) به `currentColor` بازنویسی می‌شوند.

## [2.3.0] - 2026-09-01

### Added

- کنترل «فاصلهٔ داخلی» و «گردی گوشه‌ها» برای دکمه‌های لایک و دیسلایک در ویجت Elementor، تا رنگ پس‌زمینه و کادر قابل استفاده باشند.
- ویژگی `avatar_size` برای شورت‌کد `[elin-agax-comments]`؛ کنترل «اندازهٔ تصویر نویسنده» حالا آواتار را با همان اندازه از سرور درخواست می‌کند.
- ثبت IP و شناسهٔ مرورگر برای هر دیدگاه و پاسخ، برای مدیریت و بررسی هرزنامه.
- پوشش تست برای انتشار خودکار دیدگاه مدیر، رندر پاسخ در هر سه عمق، و رد پاسخ خارج از عمق مجاز.

### Fixed

- فرم پاسخ اصلاً باز نمی‌شد. `.dadsoo-reply-form` به‌عنوان آیتم فلکسِ `.dadsoo-comment-body` فقط به اندازهٔ محتوا عرض می‌گرفت و jQuery ارتفاع بازشدن را صفر می‌خواند، پس `slideDown` روی ۰٫۲۵ پیکسل متوقف می‌ماند.
- پاسخ‌های عمق دوم و سوم هرگز نمایش داده نمی‌شدند، هرچند در دیتابیس تأییدشده بودند.
- پاسخ تازه‌ثبت‌شده با کلاس `depth-0` رندر می‌شد و هیچ تورفتگی نداشت.
- شناسهٔ پست در فرم پاسخ از اولین `input[name="post_id"]` کل صفحه خوانده می‌شد؛ اگر ویجت فرم در صفحه نبود، ثبت پاسخ با خطای «پست معتبر نیست» شکست می‌خورد.
- پاسخ تأییدنشدهٔ مهمان بلافاصله در فهرست درج می‌شد و کاربر آن را منتشرشده می‌پنداشت؛ حالا فقط پیام «در انتظار تأیید» نمایش داده می‌شود.
- فرم اصلی به‌جای کادر `.dadsoo-message` از `alert()` استفاده می‌کرد و پیام سرور را دور می‌ریخت؛ در نتیجه دیدگاه منتشرشدهٔ مدیر بدون بارگذاری دوبارهٔ صفحه دیده نمی‌شد.
- آواتار همیشه در ۴۲ پیکسل درخواست می‌شد و با بزرگ‌کردن از طریق کنترل استایل تار می‌شد.
- ورودی‌های AJAX بدون `wp_unslash` پردازش می‌شدند و بک‌اسلش اضافه در متن ذخیره می‌شد.
- تشخیص خودکار فیلد نظر روی مقدار غیررشته‌ای به خطای PHP 8 برخورد می‌کرد؛ حالا فیلد `comment` مستقیم خوانده می‌شود و فیلدهای داخلی وردپرس نادیده گرفته می‌شوند.
- اگر فقط یکی از دو حالت آیکون تنظیم می‌شد، دکمه در حالت دیگر بدون آیکون می‌ماند.
- درج پاسخ در `.dadsoo-replies` با `find()` انجام می‌شد و با تو در تو شدن پاسخ‌ها در همهٔ سطوح تکرار می‌شد.
- تورفتگی پاسخ‌ها به `margin-inline-start` تغییر کرد تا در سایت‌های راست‌به‌چپ هم سمت درست تورفته شود.

### Changed

- بیشترین عمق تو در تویی صریحاً سه سطح تعریف شد: در عمق آخر دکمهٔ پاسخ نمایش داده نمی‌شود و پاسخ عمیق‌تر از سمت سرور رد می‌شود.
- پاسخ AJAX «ثبت دیدگاه» و «ثبت پاسخ» به‌جای رشته، شیئی با کلیدهای `approved`، `message` و `html` برمی‌گرداند. اگر جاوااسکریپت این افزونه را سفارشی کرده‌اید، این تغییر ناسازگار است.

## [2.2] - 2026-07-25

### Added

- مستندات نصب و استفاده برای آماده‌سازی انتشار اولیهٔ مخزن.
- تغییر نام افزونه به Elin Agax Comment و یکپارچه‌سازی شناسه‌های فنی با نام Elinweb.
- ویجت‌های مستقل Elementor برای فرم ارسال و فهرست دیدگاه‌ها، همراه با کنترل آیکون و استایل رأی‌ها.

### Fixed

- افزودن فایل‌های پایهٔ آماده‌سازی انتشار (`README.md` و `.gitignore`).
