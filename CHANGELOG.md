# Changelog

همهٔ تغییرات مهم این پروژه در این فایل ثبت می‌شوند.

## [2.3.0] - 2026-09-01

### Added

- کنترل «فاصلهٔ داخلی» و «گردی گوشه‌ها» برای دکمه‌های لایک و دیسلایک در ویجت Elementor، تا رنگ پس‌زمینه و کادر قابل استفاده باشند.
- ویژگی `avatar_size` برای شورت‌کد `[elin-agax-comments]`؛ کنترل «اندازهٔ تصویر نویسنده» حالا آواتار را با همان اندازه از سرور درخواست می‌کند.
- ثبت IP و شناسهٔ مرورگر برای هر دیدگاه و پاسخ، برای مدیریت و بررسی هرزنامه.
- پوشش تست برای انتشار خودکار دیدگاه مدیر، رندر پاسخ در هر سه عمق، و رد پاسخ خارج از عمق مجاز.

### Fixed

- فرم پاسخ اصلاً باز نمی‌شد. `.dadsoo-reply-form` به‌عنوان آیتم فلکسِ `.dadsoo-comment-body` فقط به اندازهٔ محتوا عرض می‌گرفت و jQuery ارتفاع بازشدن را صفر می‌خواند، پس `slideDown` روی ۰٫۲۵ پیکسل متوقف می‌ماند.
- پاسخ‌های عمق دوم و سوم هرگز نمایش داده نمی‌شدند، هرچند در دیتابیس تأییدشده بودند.
- پاسخ تازه‌ثبت‌شده با کلاس `depth-0` رندر می‌شد و هیچ تورفتگی نداشت.
- شناسهٔ پست در فرم پاسخ از اولین `input[name="post_id"]` کل صفحه خوانده می‌شد؛ اگر ویجت فرم در صفحه نبود، ثبت پاسخ با خطای «پست معتبر نیست» شکست می‌خورد.
- پاسخ تأییدنشدهٔ مهمان بلافاصله در فهرست درج می‌شد و کاربر آن را منتشرشده می‌پنداشت؛ حالا فقط پیام «در انتظار تأیید» نمایش داده می‌شود.
- فرم اصلی به‌جای کادر `.dadsoo-message` از `alert()` استفاده می‌کرد و پیام سرور را دور می‌ریخت؛ در نتیجه دیدگاه منتشرشدهٔ مدیر بدون بارگذاری دوبارهٔ صفحه دیده نمی‌شد.
- آواتار همیشه در ۴۲ پیکسل درخواست می‌شد و با بزرگ‌کردن از طریق کنترل استایل تار می‌شد.
- ورودی‌های AJAX بدون `wp_unslash` پردازش می‌شدند و بک‌اسلش اضافه در متن ذخیره می‌شد.
- تشخیص خودکار فیلد نظر روی مقدار غیررشته‌ای به خطای PHP 8 برخورد می‌کرد؛ حالا فیلد `comment` مستقیم خوانده می‌شود و فیلدهای داخلی وردپرس نادیده گرفته می‌شوند.
- اگر فقط یکی از دو حالت آیکون تنظیم می‌شد، دکمه در حالت دیگر بدون آیکون می‌ماند.
- درج پاسخ در `.dadsoo-replies` با `find()` انجام می‌شد و با تو در تو شدن پاسخ‌ها در همهٔ سطوح تکرار می‌شد.
- تورفتگی پاسخ‌ها به `margin-inline-start` تغییر کرد تا در سایت‌های راست‌به‌چپ هم سمت درست تورفته شود.

### Changed

- بیشترین عمق تو در تویی صریحاً سه سطح تعریف شد: در عمق آخر دکمهٔ پاسخ نمایش داده نمی‌شود و پاسخ عمیق‌تر از سمت سرور رد می‌شود.
- پاسخ AJAX «ثبت دیدگاه» و «ثبت پاسخ» به‌جای رشته، شیئی با کلیدهای `approved`، `message` و `html` برمی‌گرداند. اگر جاوااسکریپت این افزونه را سفارشی کرده‌اید، این تغییر ناسازگار است.

## [2.2] - 2026-07-25

### Added

- مستندات نصب و استفاده برای آماده‌سازی انتشار اولیهٔ مخزن.
- تغییر نام افزونه به Elin Agax Comment و یکپارچه‌سازی شناسه‌های فنی با نام Elinweb.
- ویجت‌های مستقل Elementor برای فرم ارسال و فهرست دیدگاه‌ها، همراه با کنترل آیکون و استایل رأی‌ها.

### Fixed

- افزودن فایل‌های پایهٔ آماده‌سازی انتشار (`README.md` و `.gitignore`).
